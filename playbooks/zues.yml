---
- name: Configure Zues
  hosts: zues
  gather_facts: true

  roles:
  - role: system/windows_base
    vars:
      hostname: Zues
      users_to_rename:
      - old_username: 'astyv'
        new_username: 'andrew'
  - role: apps/syncthing_windows